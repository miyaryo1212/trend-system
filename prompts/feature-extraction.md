# Step 1: 新機能・トピック抽出プロンプト

以下のRSS/Webデータを分析し、{{CHANNEL_NAME}}に関する直近48時間以内の
新機能・アップデート・トピックを抽出してください。

## チャネル固有の指示

{{EXTRACTION_PROMPT}}

## 入力データ (RSSフィード)

```
{{RSS_DATA}}
```

## 追加調査が必要なソース

以下のソースはRSSが利用できないため、`curl` でWeb検索して最新情報を補完してください。
該当するソースがない場合はこのセクションを無視してください。

{{WEB_SEARCH_QUERIES}}

## 出力ルール

- 箇条書きのみ。前置き・補足・まとめは一切不要
- 各行は `- 名前: 簡潔な説明(1行以内)` の形式
- 該当なしの場合は `なし` とだけ出力
- 出力先: `{{FEATURES_PATH}}` にテキストファイルとして Write ツールで書き出すこと
